# ⚡ 一键强制流程补丁（防模型幻觉与记忆崩塌修复工具）

> **给 AI Agent 的执行指令**：
> 当用户让你“运行补丁”或“执行补丁”时，请立刻开启多步或并行工具调用（建议使用 `replace_file_content` 或文本替换相关的内置工具），将当前工程下的两个核心工作流文件进行防幻觉升级修改。修改完成后，请严格按照本补丁的检查机制向用户报告成功状态。

---

## 🎯 补丁执行目标与靶点位置

请在项目所在的工作区目录下（通常位于 `.agent/workflows/` 下或类似用于存放工作指南规则指令的子目录）检索定位以下两个核心工作流文件，并执行严厉的文本强制替换：

1. **目标文件1**：负责大纲与剧情规划的细纲工作流（如 `一键生成细纲.md` 或类似命名）
2. **目标文件2**：负责连续写正文的工作流（如 `一键AI续写章节_v4.0.md`，或类似命名）

---

## 🛠️ 第一阶段：升级细纲规划的强制物理读取卡口

请检索你的目标文件1，并使用精确的字符串替换或代码编辑能力，执行以下两处护城河级别的卡口嵌入：

### 替换点 A（开局记忆清零）
**寻找你看到的类似于“步骤1: 读取核心上下文”开头的部分：**
将含有“步骤1”及其列表的顶层逻辑，**强行替换为下面这段高亮约束体系**（请确保保留甚至增强对方原有的必须读取文件列表的条目与目录）：

```markdown
### 步骤1: 强制清空记忆假定与物理调用（防漏机制）

> [!CAUTION]
> **【多模型切换防崩坏机制 - 头等执行优先】**
> 你必须进行“零记忆体系假定”：无论你觉得你是否在之前的对话中见过这些文件，都**假定你现在处于丢失上下文的全新状态**。因为你极有可能是刚刚被主系统唤醒调度来承接当前步骤的全新模型实例（与其他模型间不共享记忆缓存）。
> **绝对禁止**依赖内置知识或虚假幻觉进行瞎编！
> 你**必须且只能**通过具体的 API 工具去系统文件目录下**真实物理读取**必读文件，形成刚性工具调用链。

在进行任何思考、创作和输出前，你必须排队调用内置工具（如 view_file 或 get content 等）逐一读取以下所有必读清单：
*（在替换时请保留原来的必读 MD 文件列表不变：包括但不限于时间线、最新进度、人物设定和前文细纲摘要库等文件）*

**【强制阻断点】**：在完成所有的 API 读取操作后，你必须首先在对话框内向用户输出一个带有进度勾选的**「📁 物理读取完成清单」**。在执行完物理翻书之后，才允许进入下方的思考与输出环节！
```

### 替换点 B（末尾审核把关）
**寻找原来内容结尾的审核步骤（如“步骤4.5: 自动逻辑审核与修复”）**，并将其标题及核心引导警告升级替换为：

```markdown
### 步骤X: ⚡强制逻辑审查与修复（绝对阻断点）⚡

> [!CAUTION]
> **【生成后强制拦截审核机制】**
> 本步骤为不可逾越的核心阻断点！严禁出于“AI的直觉”自认为内容没有问题而蒙混过关！
> 在审核前，**你必须第一步先触发工具真实物理读取项目的红线文件库：**
> 如 `00_核心设定/审核规则.md` （或项目中对应的其他专有审查记录表）。
> 在没有调取这个文件获得 CM 红线标准的情况下，直接打印逻辑审核报告或说自己合格了，将直接被视为极其严重的作弊与拒不执行指令。

（在替换时保留下方的强校验核查报告模板，并新增一项：✅ 物理读取《审核规则.md》：确认已调取）
```

---

## 🛠️ 第二阶段：升级正文执笔的强制物理读取卡口

请检索你的目标文件2（正文生成工作流），同样执行严厉嵌套：

### 替换点 C（正文生成前）
**找到原有的“读取核心上下文（必读清单）”内容标题及其紧随其后的警告块（如果没有警告请强行插入），替换或添加：**

```markdown
### 步骤1: 强制清空记忆假定与物理调用（防漏机制）

> [!CAUTION]
> **【多模型切换协同的红线防雷壁垒】**
> 本项目执行严密的多模型协同协作（例如另一个 AI 模型做的规划，而你现在是专门调来写正文的执笔模型）。
> 因为实例阻隔，你**绝对不继承**上一个跑路模型的上下文记忆和缓存内容！
> 因此，**绝对禁止**你根据只言片语凭空想象当前设定；也**绝对禁止**你以“我已经了解了”、“我知道”为接口跳过文档的扫页阅读环节。
> 你的第一动作，**必须首当其冲去触发工具链**，顺序向下将所有的源始依赖文件拉取回你的短期内存。

（保留原来的必读文件表格不变）

**【强制阻断点】**：只有在你通过内部工具调用（Tool Calls）确认了硬盘文档都被拉拽完成之后，才准往下生成哪怕第一段的话术！
```

### 替换点 D（入库前审核）
**找到章节生成后的自检审核部分（步骤8.5大同小异的末位检查单）**，将自检开头的声明块增强升级：

```markdown
### 步骤末位: ⚡成品大抽检与修复的极刑红线机制（入库前的绝对阻断）⚡

> [!CAUTION]
> **【完工成品后的强制复盘机制】**
> 你所有的分段都生成完并组装后，不能直接说好了收工！本步骤是保障质量的定海神针！
> 你必须调动物理工具调用系统，回去硬盘找项目的底线规则抓手。绝不能随便自己拍拍脑袋说“通过”。
> **必须第一使用工具主动提取**：项目对应的红线或“死刑审核表”（如 `审核规则.md`）。
> 在未拉取该红线核验之前，你端出来的任何内容不仅不具备合法性，且大概率含有诸如死气臭词、破折号乱飞等不可饶恕的排版错误！

（确保报告结论里要求 AI 清晰地打出：检测修复了几个臭词和非法标点）
```

---

## 🏁 AI 执行完毕确认指令（对正在执行本文件的Agent说）

当你（处理此补丁任务的 AI Agent）将工作区中的这两个文件都成功执行完字符串覆盖修补后，你必须执行以下终验：
1. 用工具读取修改后的文件片段，确认 `[!CAUTION]` 的警示块和“强制工具调用链”等词汇实打实地留在了那些流（Workflow）的控制文件上。
2. 以自信和自豪的语气回复用户：“🎉 多模型防漏防崩塌协同补丁已在本项目全系工作流中光荣注入完成！从此无论是克劳德(Claude)还是双子星(Gemini)上场，都将强制服从系统物理翻越书房，拿不到档案不准动笔干活了！”
