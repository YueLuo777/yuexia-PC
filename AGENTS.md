# 月下PC 项目协作规则

本文件适用于 `E:\0yuexia\0,月下PC` 整个仓库。进入本仓库工作的 AI 在分析、修改、验证和汇报时都必须遵守这些规则。

## 1. 项目范围

- 只修改本仓库中的文件。除非用户明确授权，不得修改 `yuexia`、`xinyuexia` 的其他副本或任何兄弟项目。
- 尊重工作区中已有的未提交改动；不得重置、覆盖或顺手整理与当前任务无关的文件。
- 修改范围应保持聚焦。发现额外问题时可以报告，但不要未经授权扩大实现范围。
- 用户在当前任务中的明确要求优先。如果新要求与本文件冲突，先向用户说明冲突和影响，不得静默忽略其中任意一方。

## 2. 新增和维护项目规则

- 当用户明确说“新增项目规则”“添加项目规则”“把这条写进项目规则”“以后这个项目都这样”或表达等价意思时，必须在同一任务中更新本文件。
- 用户给出的规则已经清楚时，直接添加，不要重复询问是否确认。
- 新规则应写成简短、明确、可执行、可验证的要求；尽量同时写明适用范围、禁止事项和完成条件。
- 添加新规则时不得擅自删除、弱化或改写已有规则。发现规则冲突时，先向用户说明，由用户决定保留方式。
- 能通过脚本、测试、Lint 或构建检查自动验证的规则，应优先增加自动化门禁，而不是只依赖文字提醒。
- 完成规则更新后，必须告诉用户新增规则所在章节和核心内容。

## 3. 连续执行与一次性完成

- 当用户说“继续”“接着做”“继续完成”或表达等价意思时，必须从当前进度直接执行并完成验证，不要重复询问已经明确的需求。
- 只有遇到无法从项目上下文判断、会明显改变用户目标、涉及破坏性操作或确实需要新增授权的事项时，才暂停并询问用户。
- 当用户说“把剩下的相关工作一起做完”“全部做完”“一次做完”或表达等价意思时，应先确认当前任务范围内的相关未完成项，并在同一任务中一并实现和验证。
- 不要把能够一起完成的相关工作故意拆成多轮，也不要在只完成分析、局部修改或临时页面后就报告结束。
- 一次性完成要求不代表可以扩大到无关问题；仍应遵守项目范围和用户给出的任务边界。

## 4. 文件大小与拆分

- `src`、`electron`、`scripts` 中的源码遵循 `scripts/check-source-size.cjs`：超过 500 行进入拆分预警，普通文件硬上限为 700 行；达到硬上限后禁止继续增长。
- React 页面和组件以 300～400 行为日常控制目标；新建普通源码文件原则上不得超过 500 行。
- 文件接近或超过 500 行时，不要求为了行数立即机械拆分，但不得继续向其中增加大型功能；新增逻辑应优先拆到组件、Hook、服务、类型或工具文件。
- 自动化测试文件可以根据用例完整性适当放宽，但不得超过 900 行；放宽只适用于 `*.test.*` 和 `*.spec.*`，不适用于测试板块中的普通页面源码。
- 禁止通过压缩代码、合并多条语句、删除必要注释或降低可读性来规避行数检查。
- 禁止未经用户明确批准提高 `HARD_LIMIT`、`TEST_HARD_LIMIT`，增加新的 `TRANSITION_LIMIT_OVERRIDES`，或提高现有临时豁免值。
- 修改已有豁免大文件时，新增逻辑应优先拆到独立文件，原则上不得让该大文件继续增长；历史豁免值只能保持或下降，不能继续提高。
- `src/features/workbench/components/WorkbenchLibraryPanel.tsx` 等历史豁免超大文件禁止继续承载新功能的大型逻辑；后续新功能必须优先拆到独立组件、Hook、model、service 或工具文件，完成时该类大文件行数原则上不得增加，并必须通过 `npm.cmd run check:source-size` 验证。
- 生成分片、样式分片和 `*.part-N.*` 文件不得超过现有检查规定的 500 行上限。
- 完成代码修改后必须运行 `npm.cmd run check:source-size`；失败时继续拆分，不得把任务报告为已完成。

## 5. 代码结构与实现质量

- 一个文件保持单一主要职责：页面负责组合，复杂状态放入 Hook，业务流程放入服务，纯逻辑放入工具函数。
- 不要在页面组件中继续堆积可独立复用的大段状态管理、数据转换或业务规则。
- 优先复用现有类型、组件、Hook 和工具；增加重复实现前先搜索仓库。
- 修复问题应处理根因，不得只隐藏报错、吞掉异常或绕开原有检查。
- 不得为了通过检查而新增 `@ts-nocheck`、随意扩大 `any`、关闭测试、降低断言或禁用关键 Lint/TypeScript 规则。
- 不得直接修改 `node_modules`、`dist`、`release` 或其他可重新生成的产物来实现源码功能。
- 不得输出、提交或在日志中暴露 `.env.local`、API Key、Token、密码及其他秘密信息。

## 6. UI、测试板块与交互约定

- 新增按钮默认使用“仅图标”或“仅文字”，不要同时使用图标和文字，除非用户明确要求。
- 新增或修改弹窗时，必须先参考项目中现有同类弹窗并复用其组件、布局、间距、圆角、阴影、标题栏、按钮和交互规范；不得脱离现有体系重新设计或单独设置一套样式，完成后应确认弹窗视觉与交互风格统一。
- 新增或修改任何 UI 前，必须先搜索项目内相同业务大类和相同界面层级的正式实现，选定现有主流实现作为基准；优先复用已有组件、布局骨架和样式常量，不得因开发时间、页面入口或功能分支不同而另起一套视觉版本。
- 同一业务大类下的页面、侧栏、列表、卡片、标签、表单、空状态、选择器和操作区，应统一布局、间距、尺寸、圆角、颜色、选中态、按钮位置和基础交互；确有业务差异时，只保留必要差异，共同部分仍必须复用，并在完成报告中说明差异理由。
- 统一或调整某个同类 UI 时，必须同时检查其兄弟页面和其他入口，避免只修改其中一个版本；适合源码断言、组件测试或视觉回归测试的，应增加门禁，确认同类界面不会再次分叉后才能报告完成。
- 点击行为必须清楚区分“预览”和“选择”。人物状态、道具状态、章节梗概等素材行或卡片，点击只负责预览；关联或选择必须使用明确的复选框或选择控件。
- 当用户说“做到测试里”“放到测试里”“先在测试里做”或表达等价意思时，“测试”特指应用内的测试板块，即 `TestCollectionPage` 及其测试页面和路由；不能只增加 Vitest 等自动化测试文件就认为已经完成。
- 当用户说“删除测试”“把这个测试删掉”或表达等价意思时，表示该临时测试已经验证完成，应删除测试板块里的对应测试页面、组件文件、入口、懒加载导入和路由分支。
- 删除测试板块里的临时测试时，应保留已经迁移到正式页面的功能成果；除非用户明确要求删除自动化测试，否则不得把“删除测试”解释为删除必要的 Vitest 回归测试。
- 修改正式页面前，可以使用测试页面验证复杂交互；验证完成并迁移到正式页面后，应在同一任务中删除临时页面、路由和测试集合入口。
- 向 `TestCollectionPage` 添加测试页面或入口时，放在对应分组的末尾，保持既有顺序稳定。
- 用户要求删除或移出正式导航的功能时，应清理正式入口、路由配置和持久化导航残留，不能只用 CSS 隐藏。
- UI 修复必须验证用户实际可见的结果；不能只验证内部状态、占位文字或单个局部实现。

## 7. Bug 修复与错误日志

- 修复 Bug、回归问题或 UI 行为问题时，同时更新应用内错误日志。
- 错误日志至少记录：现象、原因、修复方式、以后如何预防。
- 能稳定复现的问题应增加有针对性的回归测试；确实不适合自动化时，在结果中说明实际验证方式。
- 不得只根据 `launcher.log` 或单个 HTTP 结果判断桌面程序启动成功；涉及启动器时还要验证 Electron 进程和实际窗口启动链路。

## 8. 验证要求

- 本项目是 PC 桌面软件。自动化检查、类型检查、单元测试、构建成功、Vite 页面可访问或 HTTP 200，只能证明对应检查通过，不等于功能已经真实可用，也不得单独作为“交付完成”的依据。
- 开发过程中先运行与修改范围直接相关的测试或检查，以便快速发现问题。
- 一般代码修改完成后至少运行：
  1. `npm.cmd run check:source-size`
  2. `npm.cmd run check`
  3. `npm.cmd run test:run`
- 任务交付前优先运行 `npm.cmd run verify:quick`；涉及生产构建、路由、打包或最终 UI 资源时，再运行 `npm.cmd run build`。
- 涉及 Electron、预加载脚本或启动器时，还要运行对应的 `npm.cmd run check:electron`、`npm.cmd run check:launcher`，并按任务风险进行真实桌面启动验证。
- 除纯文档、纯测试代码或明确不影响运行行为的内部重构外，修改功能、Bug、UI、交互、路由、数据保存、导入导出、启动器或打包流程后，交付前必须启动真实桌面应用，并从用户正常入口完成至少一次与本次需求直接对应的端到端操作；不得只检查源码、组件内部状态、测试页、开发服务器或日志。
- 真实桌面验收必须覆盖本次改动的关键用户路径，并在开始前写明或在报告中列明：进入路径、操作步骤、预期结果和实际结果。涉及保存或持久化时，应验证保存后刷新或重启；涉及异常处理时，应验证至少一个相关失败场景；涉及多个正式入口或兄弟页面时，应检查受影响的其他入口。
- Electron 桌面启动验收至少确认：Electron 进程真实存在、应用窗口真实打开、目标页面可见且不是白屏、目标功能能从正式入口进入并完成关键操作。只存在进程、日志、端口监听或 HTTP 200 均不能单独判定启动或功能验收通过。
- 涉及 UI、布局、弹窗、点击、拖拽、键盘、焦点或其他可见交互时，必须在真实桌面窗口中亲自操作并观察用户可见结果。应在遵守第 9 节多屏保护规则的前提下，为关键初始状态和结果状态提供窗口级或目标区域截图；单张截图无法证明连续交互时，还必须记录实际操作步骤和逐步观察结果，必要时使用仅包含副屏目标窗口的录屏或多张截图。
- 真实桌面验收必须同时检查“需求是否实现”和“最终效果是否做好”，不能只确认目标元素、功能或导线已经出现。验收时应检查完整受影响区域及其相邻区域，重点排查重复元素或重复线条、叠线、凸线、断线、遮挡、错位、间距不一致、颜色或层级异常等可见瑕疵；发现任一非预期现象必须标记为 `FAIL`，继续修复并重新验收，不得以“功能已经做出”为由判定通过。
- 截图只是验证证据之一，不能替代点击、输入、保存、刷新、重启、异常路径或打包版本等真实操作。证据必须能够对应本次需求的关键断言；不得使用无关页面、仅源码截图或仅终端命令成功截图冒充功能验收证据。
- 每个验收项必须明确标记为 `PASS`、`FAIL` 或 `NOT VERIFIED`：只有实际执行且结果符合预期才能标记 `PASS`；发现问题必须标记 `FAIL` 并继续修复、重新验证；受环境、权限、设备或工具限制而没有实际执行的项目必须标记 `NOT VERIFIED`，说明原因和风险，不得写成“已验证通过”。
- 发现真实验收结果与自动化检查不一致时，以真实用户可见行为为交付判断依据；应先修复根因，再重新运行相关自动化检查和完整用户路径验收，不得因基础命令通过而忽略实际 Bug。
- 纯文档或仅更新 `AGENTS.md` 的任务可以不运行完整代码验证，但必须检查文件内容和 Git 差异。
- 不得声称未实际运行的命令已经通过。因为环境原因无法运行时，明确说明未验证项和原因。

## 9. 多屏测试与用户操作保护

- 进行桌面应用、浏览器或其他可视化界面测试时，应把被测窗口放在 Codex 当前所在的副屏，并只在该副屏完成点击、输入、窗口调整和结果观察。
- 截图、录屏或视觉比对只允许针对副屏上的被测窗口或目标区域，不得截取主屏内容，也不要使用包含主屏的整桌面截图。
- 测试过程中不得移动、缩放、覆盖或关闭用户主屏上的窗口，不得把被测应用主动切换到主屏。
- 应优先使用不会抢占主屏焦点、不会干扰用户鼠标和键盘操作的验证方式；需要可见交互时，也应限制在副屏并尽量减少焦点抢占时间。
- 开始可视化测试前，应先确认 Codex 窗口所在显示器，并将其作为测试目标屏幕；不要依赖固定的左右屏坐标或默认主屏编号。
- 如果无法可靠识别副屏、无法把被测窗口限制在 Codex 所在屏幕，或测试工具一定会干扰主屏操作，应暂停可视化操作并说明原因，改用无界面、窗口级或其他不干扰用户的验证方式。

## 10. 完成报告

- 完成报告面向普通用户表达，默认不罗列修改文件、代码位置、类名、函数名等技术实现细节；只有用户明确询问技术细节时再提供。
- 必须用直白语言说明最终改成了什么样、用户能看到哪些变化，以及如何进入对应页面、执行哪些操作、看到什么结果才算验收通过。
- 报告实际运行过的检查命令及结果。
- 完成报告必须将“自动化检查”和“真实桌面验收”分开列出，不得用自动化检查通过代替真实桌面验收结论。
- “真实桌面验收”至少列出：启动方式、正式入口、实际操作步骤、预期结果、实际结果、`PASS`/`FAIL`/`NOT VERIFIED` 结论，以及关键截图、录屏或其他证据的位置。没有执行的步骤必须进入“未验证项目”，并说明原因与可能风险。
- 只有本次任务要求的自动化检查通过、关键用户路径真实操作通过，且没有影响交付的 `FAIL` 或关键 `NOT VERIFIED` 时，才能声称“交付完成”或“已验证通过”；否则必须准确报告为部分完成、验证受限或仍需修复。
- 涉及文件拆分时，说明拆出了什么以及主要文件当前行数。
- 有剩余风险、临时兼容逻辑或历史豁免时必须明确指出，不得隐藏。
